<?xml version="1.0" encoding="UTF-8"?>
<project>
  <!-- ant-contrib v1.0b3, http://ant-contrib.sourceforge.net/ -->
  <taskdef resource="net/sf/antcontrib/antlib.xml" classpath="${java.class.path}" />

  <property name="HOME_DIR_tmp" location="${basedir}/.." />
  <propertyregex property="HOME_DIR" input="${HOME_DIR_tmp}" regexp="\\" replace="/" global="true" defaultValue="${HOME_DIR_tmp}" />
</project>
